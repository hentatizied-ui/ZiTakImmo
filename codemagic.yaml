workflows:
  ios-build:
    name: iOS Build
    instance_type: mac_mini_m2
    environment:
      flutter: stable
    scripts:
      - name: Build
        script: flutter build ios --release --no-codesign
    artifacts:
      - build/ios/iphoneos/Runner.app